[project]
name = "VaultStream-stack"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["arete-sdk>=0.20.1"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["vaultstream_stack*"]
